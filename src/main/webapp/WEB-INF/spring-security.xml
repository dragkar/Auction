<?xml version="1.0" encoding="UTF-8"?>

<beans:beans xmlns="http://www.springframework.org/schema/security"

             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"

             xmlns:beans="http://www.springframework.org/schema/beans"

             xsi:schemaLocation="

    http://www.springframework.org/schema/security

    http://www.springframework.org/schema/security/spring-security-4.2.xsd

    http://www.springframework.org/schema/beans

    http://www.springframework.org/schema/beans/spring-beans-4.2.xsd">


    <http auto-config="true" use-expressions="true">
        <!--<intercept-url pattern="/" access="permitAll()"/>-->
<intercept-url pattern="/mylots" access="isAuthenticated()" />
        <intercept-url pattern="/profile" access="hasRole('ROLE_USER')" />
        <csrf disabled="true"/>
        <form-login
            login-processing-url="/signin"
            username-parameter="username"
            password-parameter="password"
            login-page="/signin"
            default-target-url="/index"
            always-use-default-target="true"
            authentication-failure-url="/signin?message=Введена фигня, попробуй еще раз или свали !!! "/>
        <logout/>

    </http>

    <beans:bean id="provider" class="web.services.SpringSecurityAuth"/>
    <authentication-manager>
        <authentication-provider ref="provider"/>
    </authentication-manager>

</beans:beans>